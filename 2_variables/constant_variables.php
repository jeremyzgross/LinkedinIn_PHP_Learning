<?php
  define ('VERSION', 1.1); //define makes the variable constant and used as a function
  //in () is the name, and after comma is the value. It can then be treated as a variable without $
  //the variable is immutable
  echo VERSION; 

  //RESERVED WORDS

  //can't use certain words when making variables liek GET (more in the documentation)
?>